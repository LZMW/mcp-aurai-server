# 上级AI API密钥
# 从对应提供商的开放平台获取
AURAI_API_KEY=your-api-key-here

# AI服务提供商 (可选，默认: zhipu)
# 支持的值: zhipu, openai, anthropic, gemini, custom
AURAI_PROVIDER=zhipu

# 模型名称 (可选，默认: glm-4-flash)
# 智谱AI: glm-4-flash, glm-4, glm-4-plus, glm-4-0520
# OpenAI: gpt-4, gpt-4-turbo, gpt-3.5-turbo, gpt-4o
# Anthropic: claude-3-5-sonnet-20241022, claude-3-5-haiku-20241022
# Gemini: gemini-pro, gemini-1.5-pro, gemini-1.5-flash
# 注意: 所有模型列表可通过API动态获取，请使用配置工具的"刷新模型"功能
AURAI_MODEL=glm-4-flash

# API基础URL (可选，用于代理或自定义端点)
# AURAI_BASE_URL=https://api.example.com

# 最大迭代次数 (可选，默认: 10)
# AURAI_MAX_ITERATIONS=10

# 温度参数 (可选，默认: 0.7)
# AURAI_TEMPERATURE=0.7

# 最大生成tokens (可选，默认: 4096)
# AURAI_MAX_TOKENS=4096

# 服务器日志级别 (可选，默认: INFO)
# AURAI_LOG_LEVEL=INFO

# 启用对话历史持久化 (可选，默认: true)
# 设置为 false 可禁用持久化，仅使用内存模式
# AURAI_ENABLE_PERSISTENCE=true

# 对话历史文件路径 (可选，默认: .mcp-aurai/history.json)
# 支持相对路径或绝对路径
# 相对路径相对于当前工作目录
# AURAI_HISTORY_PATH=.mcp-aurai/history.json
